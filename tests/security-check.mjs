import{readFileSync}from'node:fs';import{execFileSync}from'node:child_process';const files=execFileSync('git',['ls-files'],{encoding:'utf8'}).trim().split('\n').filter(Boolean);const forbidden=[/service_role/i,/sb_secret_[A-Za-z0-9_-]+/,/WHATSAPP_TOKEN\s*[:=]\s*["'][^$]/,/access_token\s*[:=]\s*["'][A-Za-z0-9]/];const hits=[];for(const file of files){if(/package-lock\.json$|security-check\.mjs$/.test(file))continue;let text;try{text=readFileSync(file,'utf8')}catch{continue}for(const pattern of forbidden)if(pattern.test(text))hits.push(`${file}: ${pattern}`)}if(hits.length)throw new Error('Posibles secretos detectados:\n'+hits.join('\n'));console.log('Sin credenciales privadas expuestas');
