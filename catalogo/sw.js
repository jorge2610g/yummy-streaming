const CACHE="yummypro-streaming-catalog-v130";
const CORE=["/catalogo/","/catalogo/manifest.webmanifest","/icon-192.png","/icon-512.png","/pwa-icon.svg"];
self.addEventListener("install",event=>{event.waitUntil(caches.open(CACHE).then(c=>c.addAll(CORE)).then(()=>self.skipWaiting()))});
self.addEventListener("activate",event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith("yummypro-streaming-catalog-")&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});
self.addEventListener("message",event=>{if(event.data?.type==="SKIP_WAITING")self.skipWaiting()});
self.addEventListener("fetch",event=>{const req=event.request;if(req.method!=="GET")return;const url=new URL(req.url);if(url.origin!==self.location.origin)return;if(req.mode==="navigate"){event.respondWith(fetch(req).then(async res=>{if(res?.ok){const cache=await caches.open(CACHE);await cache.put(req,res.clone())}return res}).catch(()=>caches.match(req).then(hit=>hit||caches.match("/catalogo/"))));return}if(CORE.includes(url.pathname)){event.respondWith(fetch(req,{cache:"no-store"}).then(async res=>{if(res?.ok){const cache=await caches.open(CACHE);await cache.put(req,res.clone())}return res}).catch(()=>caches.match(req)))}});